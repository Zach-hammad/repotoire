name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            name: repotoire-linux-x86_64
          - target: x86_64-apple-darwin
            os: macos-latest
            name: repotoire-macos-x86_64
          - target: aarch64-apple-darwin
            os: macos-latest
            name: repotoire-macos-aarch64
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            name: repotoire-windows-x86_64.exe

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-action@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cmake (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y cmake build-essential

      - name: Install cmake (macOS)
        if: matrix.os == 'macos-latest'
        run: brew install cmake

      - name: Build
        run: |
          cd repotoire-cli
          cargo build --release --target ${{ matrix.target }}

      - name: Package (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          cd repotoire-cli/target/${{ matrix.target }}/release
          tar czf ../../../${{ matrix.name }}.tar.gz repotoire
          
      - name: Package (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          cd repotoire-cli/target/${{ matrix.target }}/release
          7z a ../../../${{ matrix.name }}.zip repotoire.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: repotoire-cli/${{ matrix.name }}.*

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/**/*
          generate_release_notes: true
