[project]
name = "simple-python"
version = "0.1.0"
description = "A simple Python project for testing"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "N", "W", "B"]
