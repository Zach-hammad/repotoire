/**
 * Stub ClerkProvider — auth removed (CLI-only product).
 * Kept as passthrough to avoid breaking layout.tsx imports.
 */
export function ClerkProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
